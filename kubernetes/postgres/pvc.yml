#para el volumen persistente
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: fintech

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi
