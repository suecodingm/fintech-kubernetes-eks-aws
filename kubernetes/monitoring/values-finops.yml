grafana:
  service:
    type: NodePort
    nodePort: 31668

alertmanager:
  enabled: false

prometheus:
  prometheusSpec:
    scrapeInterval: "60s"
    evaluationInterval: "60s"
    retention: "3h"
    resources:
      requests:
        memory: "150Mi"
      limits:
        memory: "300Mi"
